# Статус приведения к документации

Дата последнего обновления: 2026-06-14 16:37 CEST

## Выполнено

* [x] AI Persona email отображается как `name@sh-inc.ru`.
* [x] Memory Engine нормализует source-backed Persona facts.
* [x] Memory Engine собирает entity context pack с source citations.
* [x] Memory Engine выявляет required fact gaps.
* [x] Memory Engine выявляет stale-memory review candidates.
* [x] Memory Engine собирает cross-domain context pack для root entity и связанных сущностей.
* [x] Timeline Engine выполняет bounded timeline assembly и event-log projection runner.
* [x] Выполнен первичный аудит расхождений между документацией и реализацией для mail, Telegram и frontend decomposition.
* [x] `backend/src/domains/mail/handlers/mod.rs` разделен на bounded handler modules; все файлы в `backend/src/domains/mail/handlers/` меньше 700 строк.
* [x] Создан план architecture-gate работ: `docs/superpowers/plans/2026-06-13-documentation-alignment-architecture-gates.md`.
* [x] Восстановлены текущие architecture/code boundary guards и backend lint gate, выявленные pre-commit hook.
* [x] `frontend/src/lib/components/shared/AccountSetupModal.svelte` сокращен с 1219 до 97 строк и оставлен как modal shell/router.
* [x] Mail, Calendar, Telegram и WhatsApp setup flows вынесены в `frontend/src/lib/components/account-setup/`; все новые account setup компоненты меньше 500 строк.
* [x] `frontend/src/lib/pages/telegram/TelegramPage.svelte` сокращен с 842 до 476 строк; header, action rail и status messages вынесены в focused Telegram widgets.
* [x] `frontend/src/lib/pages/settings/widgets/AISettingsControlCenter.svelte` сокращен с 622 до 65 строк; AI provider, routing, prompt studio, runs, header, tabs, status и rail вынесены в focused widgets.
* [x] `frontend/src/lib/pages/communications/widgets/CommunicationsMessageDetail.svelte` сокращен с 550 до 318 строк; message body, attachments, headers, related actions и timeline вынесены в focused communication widgets.
* [x] Communications-owned CSS вынесен из `frontend/src/lib/pages/pages.css` в `frontend/src/lib/pages/communications/communications*.css`; root `pages.css` сокращен с 6852 до 5822 строк, новые communication CSS chunks меньше 700 строк.
* [x] Telegram-owned CSS вынесен из `frontend/src/lib/pages/pages.css` в `frontend/src/lib/pages/telegram/telegram*.css`, а Telegram QR setup styles — в `frontend/src/lib/components/account-setup/telegramQr.css`; root `pages.css` сокращен до 4737 строк, новые Telegram CSS chunks меньше 700 строк.
* [x] Account setup, shared setup controls, account modal и compose review CSS вынесены из `frontend/src/lib/pages/pages.css` в `frontend/src/lib/components/account-setup/accountSetup.css` и `frontend/src/lib/components/shared/*`; root `pages.css` сокращен до 4259 строк, новые CSS chunks меньше 700 строк.
* [x] Settings, appearance, integrations, AI settings и shared `HermesSelect` CSS вынесены из `frontend/src/lib/pages/pages.css` в owner-файлы `frontend/src/lib/pages/settings/*.css` и `frontend/src/lib/components/shared/hermesSelect.css`; root `pages.css` сокращен до 2540 строк, новые CSS chunks меньше 700 строк.
* [x] Agents-owned CSS вынесен из `frontend/src/lib/pages/pages.css` в `frontend/src/lib/pages/agents/agents.css`; root `pages.css` сокращен до 2324 строк, новый CSS chunk меньше 700 строк.
* [x] Calendar-owned CSS вынесен из `frontend/src/lib/pages/pages.css` в `frontend/src/lib/pages/calendar/calendar.css`; root `pages.css` сокращен до 2042 строк, новый CSS chunk меньше 700 строк.
* [x] Documents/Notes-owned CSS вынесен из `frontend/src/lib/pages/pages.css` и `frontend/src/lib/styles/app.css` в owner-файлы `frontend/src/lib/pages/documents/documents.css` и `frontend/src/lib/pages/notes/notes.css`; root `pages.css` сокращен до 1912 строк, `app.css` сокращен до 1008 строк, новые CSS chunks меньше 700 строк.
* [x] Projects-owned CSS вынесен из `frontend/src/lib/pages/pages.css` и `frontend/src/lib/styles/app.css` в `frontend/src/lib/pages/projects/projects.css`; root `pages.css` сокращен до 1778 строк, `app.css` сокращен до 998 строк, новый CSS chunk меньше 700 строк.
* [x] Tasks-owned CSS вынесен из `frontend/src/lib/pages/pages.css` и `frontend/src/lib/styles/app.css` в `frontend/src/lib/pages/tasks/tasks.css`; root `pages.css` сокращен до 1628 строк, `app.css` сокращен до 995 строк, новый CSS chunk меньше 700 строк.
* [x] Persons-owned CSS вынесен из `frontend/src/lib/pages/pages.css` и `frontend/src/lib/styles/app.css` в `frontend/src/lib/pages/persons/persons.css`; root `pages.css` сокращен до 1416 строк, `app.css` сокращен до 990 строк, новый CSS chunk меньше 700 строк.
* [x] Timeline-owned CSS вынесен из `frontend/src/lib/pages/pages.css` и `frontend/src/lib/styles/app.css` в `frontend/src/lib/pages/timeline/timeline.css`; неиспользуемый `timeline-slider` CSS удален; root `pages.css` сокращен до 1349 строк, `app.css` сокращен до 987 строк, новый CSS chunk меньше 700 строк.
* [x] Organizations-owned CSS вынесен из `frontend/src/lib/pages/pages.css` и `frontend/src/lib/styles/app.css` в `frontend/src/lib/pages/organizations/organizations.css`; root `pages.css` сокращен до 1178 строк, `app.css` сокращен до 985 строк, новый CSS chunk меньше 700 строк.
* [x] Knowledge/Review-owned CSS вынесен из `frontend/src/lib/pages/pages.css` и `frontend/src/lib/styles/app.css` в `frontend/src/lib/pages/knowledge/knowledge.css` и `frontend/src/lib/pages/review/review.css`; root `pages.css` сокращен до 457 строк и больше не является God File, `app.css` сокращен до 973 строк, новые CSS chunks меньше 700 строк.
* [x] Sidebar Settings-owned CSS вынесен из `frontend/src/lib/components/shell/sidebar.css` в `frontend/src/lib/pages/settings/widgets/sidebarSettings.css`; `sidebar.css` сокращен до 590 строк и больше не является God File, новый CSS chunk содержит 270 строк.
* [x] Shared panel/editor/strip CSS и page-owned panel selectors вынесены из `frontend/src/lib/components/shared/panels.css` в owner-файлы компонентов и страниц; `panels.css` сокращен с 1780 до 697 строк и больше не является God File, новые CSS chunks меньше 700 строк.
* [x] Shell layout и shell theme CSS вынесены из `frontend/src/lib/styles/app.css` в `frontend/src/lib/styles/shell.css` и `frontend/src/lib/styles/shellTheme.css`; `app.css` сокращен с 973 до 640 строк и больше не является God File.
* [x] Communications store разделен на bounded modules `state`, `loaders`, `compose`, `actions`, `selectors` и `formatters`; `frontend/src/lib/stores/communications.ts` сокращен с 899 до 5 строк и оставлен как public facade, все store modules меньше 700 строк.
* [x] Accounts service разделен на bounded modules `calendar`, `drawer`, `labels`, `mailImport`, `mailSetup`, `mailWizard`, `shared`, `telegram` и `types`; `frontend/src/lib/services/accounts.ts` сокращен с 1011 до 9 строк и оставлен как public facade, все account service modules меньше 700 строк.
* [x] Communications service разделен на bounded modules `actions`, `compose`, `constants`, `formatters`, `loaders`, `related`, `rendering`, `resources`, `types`, `workbench` и `workflow`; `frontend/src/lib/services/communications.ts` сокращен с 1437 до 11 строк и оставлен как public facade, все communication service modules меньше 700 строк.
* [x] Telegram service разделен на bounded modules `automation`, `calls`, `constants`, `fixtures`, `lifecycle`, `messages`, `parsing`, `runtime`, `selection`, `types`, `wizard` и `workspace`; `frontend/src/lib/services/telegram.ts` сокращен с 1584 до 11 строк и оставлен как public facade, все Telegram service modules меньше 700 строк.
* [x] API contract types разделены на bounded modules `accounts`, `ai`, `calendar`, `communication`, `contradictions`, `decisions`, `documents`, `graph`, `mail`, `obligations`, `organizations`, `persons`, `projects`, `relationships`, `settings`, `tasks`, `telegram`, `vault` и `whatsapp`; `frontend/src/lib/api/types.ts` сокращен с 2615 до 19 строк и оставлен как public facade, все API type modules меньше 700 строк.
* [x] Telegram TDLib JSON boundary разделен на bounded modules `client`, `identifiers`, `library_paths`, `parsing`, `qr_login`, `qr_login_support`, `requests` и `snapshots`; `backend/src/integrations/telegram/tdjson.rs` сокращен с 2361 до 603 строк и оставлен как public facade, все TDLib modules меньше 700 строк.
* [x] Telegram client/store boundary разделен на bounded modules `accounts`, `chats`, `errors`, `identifiers`, `messages`, `models`, `projection`, `rows`, `store`, `validation` и `vault`; `backend/src/integrations/telegram/client.rs` сокращен с 1793 до 36 строк и оставлен как public facade, все новые client modules меньше 700 строк.
* [x] Telegram runtime boundary разделен на bounded modules `actor`, `commands`, `manager`, `media`, `models`, `state`, `status` и `validation`; `backend/src/integrations/telegram/runtime.rs` сокращен с 1538 до 22 строк и оставлен как public facade, все новые runtime modules меньше 700 строк.
* [x] Mail background sync boundary разделен на bounded modules `service`, `provider`, `store`, `models`, `errors`, `rows` и `validation`; `backend/src/domains/mail/background_sync.rs` сокращен с 1684 до 24 строк и оставлен как public facade, все новые background sync modules меньше 700 строк.
* [x] Calendar handlers boundary разделен на bounded modules `accounts`, `events`, `intelligence`, `meetings`, `scheduling`, `health`, `brain`, `search`, `rules`, `sync`, `reminders` и `analytics`; `backend/src/domains/calendar/handlers/mod.rs` сокращен с 1674 до 171 строки и оставлен как crate-local facade, все calendar handler modules меньше 700 строк.
* [x] Shared API support boundary разделен на bounded modules `stores`, `platform_dtos`, `communications`, `review_lists`, `messaging_integrations`, `automation_calls`, `review_commands`, `query_parsing` и `formatting`; `backend/src/domains/api_support.rs` сокращен с 1762 до 171 строки и оставлен как crate-local facade, все новые API support modules меньше 700 строк.
* [x] App error boundary разделен на bounded modules `types`, `response`, `conversions` и focused response mappings; `backend/src/app/error.rs` сокращен с 1720 до 5 строк и оставлен как crate-local facade, все новые app error modules меньше 700 строк.
* [x] AI Control Center boundary разделен на bounded modules `store`, `providers`, `catalog`, `routes`, `prompts`, `vault`, `models`, `errors`, `rows`, `presets` и `validation`; `backend/src/ai/control_center.rs` сокращен с 1908 до 25 строк и оставлен как public facade, все новые AI Control Center modules меньше 700 строк.
* [x] AI Core boundary разделен на bounded modules `semantic`, `runs`, `service`, `types`, `agents`, `prompts`, `helpers`, `errors` и `constants`; `backend/src/ai/core.rs` сокращен с 1889 до 24 строк и оставлен как public facade, все новые AI Core modules меньше 700 строк.
* [x] Persons handlers boundary разделен на bounded modules `profile`, `identity`, `compatibility`, `memory`, `intelligence`, `health`, `investigator`, `history`, `errors` и `support`; `backend/src/domains/persons/handlers/mod.rs` сокращен с 1467 до 19 строк и оставлен как crate-local facade, все новые person handler modules меньше 700 строк.
* [x] App router boundary разделен на route groups `status_vault`, `communications`, `knowledge`, `persons`, `calendar`, `organizations`, `tasks`, `review`, `settings`, `ai`, `messaging`, `email_accounts`, `audit_events` и `public`; `backend/src/app/router.rs` сокращен с 1249 до 211 строк и оставлен как lifecycle/router facade, все новые route modules меньше 700 строк.
* [x] Consistency / Contradiction Engine boundary разделен на bounded modules `store`, `engine`, `models`, `errors`, `helpers`, `parsing`, `rows`, `evidence`, `validation` и `constants`; `backend/src/engines/consistency.rs` сокращен с 1240 до 20 строк и оставлен как public facade, все новые consistency modules меньше 700 строк.
* [x] Platform settings boundary разделен на bounded modules `store`, `definitions`, `models`, `persistence`, `validation`, `ai_runtime`, `errors` и `constants`; `backend/src/platform/settings.rs` сокращен с 1171 до 13 строк и оставлен как public facade, все новые settings modules меньше 700 строк.
* [x] Task candidates boundary разделен на bounded modules `store`, `models`, `events`, `extraction`, `persistence`, `validation`, `ids`, `errors` и `constants`; `backend/src/domains/tasks/candidates.rs` сокращен с 1156 до 16 строк и оставлен как public facade, все новые task candidate modules меньше 700 строк.
* [x] Mail messages boundary разделен на bounded modules `store`, `models`, `states`, `projection`, `rows`, `payload`, `ids`, `validation` и `errors`; `backend/src/domains/mail/messages.rs` сокращен с 1149 до 20 строк и оставлен как public facade, все новые mail message modules меньше 700 строк.
* [x] Decisions boundary разделен на bounded modules `store`, `candidate_refresh`, `models`, `graph_projection`, `row_mapping`, `ids`, `validation`, `errors` и `constants`; `backend/src/domains/decisions/mod.rs` сокращен с 1115 до 19 строк и оставлен как public facade, все новые decision modules меньше 700 строк.
* [x] Document processing boundary разделен на bounded modules `store`, `jobs`, `runner`, `retry`, `artifacts`, `documents`, `models`, `rows`, `ids`, `validation`, `errors` и `constants`; `backend/src/domains/documents/processing.rs` сокращен с 1103 до 20 строк и оставлен как public facade, все новые document processing modules меньше 700 строк.
* [x] Mail core ingestion boundary разделен на bounded modules `accounts`, `raw_records`, `checkpoints`, `secrets`, `models`, `rows`, `validation`, `errors` и `store`; `backend/src/domains/mail/core.rs` сокращен с 1048 до 20 строк и оставлен как public facade, все новые mail core modules меньше 700 строк.
* [x] Projects core boundary разделен на bounded modules `store`, `read_model`, `projection`, `models`, `rows`, `ids`, `validation`, `errors` и `constants`; `backend/src/domains/projects/core.rs` сокращен с 977 до 18 строк и оставлен как public facade, все новые project core modules меньше 700 строк.
* [x] Host Vault boundary разделен на bounded modules `lifecycle`, `secrets`, `storage`, `manifest`, `recovery`, `key_store`, `crypto`, `files`, `models`, `errors`, `paths` и `constants`; `backend/src/vault/mod.rs` сокращен с 953 до 32 строк и оставлен как public facade, все новые vault modules меньше 700 строк.
* [x] Obligations domain boundary разделен на bounded modules `store`, `models`, `graph_projection`, `row_mapping`, `ids`, `validation` и `errors`; `backend/src/domains/obligations/mod.rs` сокращен с 933 до 17 строк и оставлен как public facade, все новые obligation modules меньше 700 строк.
* [x] Graph core boundary разделен на bounded modules `store`, `queries`, `models`, `row_mapping`, `ids`, `validation`, `errors` и `constants`; `backend/src/domains/graph/core.rs` сокращен с 928 до 18 строк и оставлен как public facade, все новые graph core modules меньше 700 строк.
* [x] Mail account setup boundary разделен на bounded modules `service`, `models`, `vault`, `helpers`, `validation`, `errors` и `constants`; `backend/src/domains/mail/accounts.rs` сокращен с 906 до 14 строк и оставлен как public facade, все новые account setup modules меньше 700 строк.
* [x] `email_account_setup` regression suite стабилизирован для host-vault startup reconciliation: тест теперь ожидает восстановление provider-account secret binding так же явно, как account, secret reference и calendar account.
* [x] Organization handlers boundary разделен на bounded modules `organizations`, `core_records`, `workflows`, `finance`, `enrichment`, `health`, `investigator` и `support`; `backend/src/domains/organizations/handlers/mod.rs` сокращен с 906 до 16 строк и оставлен как crate-local facade, все новые organization handler modules меньше 700 строк.
* [x] Task handlers boundary разделен на bounded modules `tasks`, `core_records`, `intelligence`, `providers`, `rules`, `health`, `candidates` и `support`; `backend/src/domains/tasks/handlers/mod.rs` сокращен с 898 до 16 строк и оставлен как crate-local facade, все новые task handler modules меньше 700 строк.
* [x] Gmail provider network client boundary разделен на bounded modules `gmail_api`, `imap`, `options`, `models`, `helpers` и `errors`; `backend/src/integrations/gmail/client.rs` сокращен с 897 до 11 строк и оставлен как public facade, все новые Gmail client modules меньше 700 строк.
* [x] Relationships domain boundary разделен на bounded modules `store`, `models`, `graph_projection`, `row_mapping`, `ids`, `validation` и `errors`; `backend/src/domains/relationships/mod.rs` сокращен с 893 до 17 строк и оставлен как public facade, все новые relationship modules меньше 700 строк.
* [x] Platform secrets boundary разделен на bounded modules `store`, `models`, `resolver`, `file_vault`, `database_vault`, `crypto`, `validation`, `errors` и `paths`; `backend/src/platform/secrets.rs` сокращен с 866 до 19 строк и оставлен как public facade, все новые secrets modules меньше 700 строк.
* [x] Person identity resolution boundary разделен на bounded modules `store`, `models`, `events`, `upsert`, `rows`, `validation`, `errors` и `constants`; `backend/src/domains/persons/identity.rs` сокращен с 834 до 15 строк и оставлен как public facade, все новые identity modules меньше 700 строк.
* [x] Mail storage boundary разделен на bounded modules `blob_store`, `store`, `models`, `scanner`, `rows`, `validation`, `ids`, `errors` и `constants`; `backend/src/domains/mail/storage.rs` сокращен с 815 до 21 строки и оставлен как public facade, все новые storage modules меньше 700 строк.
* [x] Project link review boundary разделен на bounded modules `store`, `models`, `events`, `adapters`, `rows`, `target_checks`, `validation`, `errors` и `constants`; `backend/src/domains/projects/link_reviews.rs` сокращен с 806 до 16 строк и оставлен как public facade, все новые link review modules меньше 700 строк.
* [x] Mail RFC822/MIME parser boundary разделен на bounded modules `parser`, `models`, `headers`, `decoding`, `multipart`, `body`, `wire`, `util` и `errors`; `backend/src/domains/mail/rfc822.rs` сокращен с 768 до 13 строк и оставлен как public facade, все новые RFC822 modules меньше 700 строк.
* [x] Platform config boundary разделен на bounded modules `app_config`, `ai`, `google`, `parsing`, `errors` и `constants`; `backend/src/platform/config.rs` сокращен с 711 до 11 строк и оставлен как public facade, все новые config modules меньше 700 строк.
* [x] WhatsApp Web client boundary разделен на bounded modules `store`, `models`, `projection`, `rows`, `ids`, `validation`, `errors` и `constants`; `backend/src/integrations/whatsapp/client.rs` сокращен с 709 до 17 строк и оставлен как public facade, все новые WhatsApp client modules меньше 700 строк.
* [x] Graph projection boundary разделен на bounded modules `service`, `persons`, `messages`, `documents`, `projects`, `models`, `rows`, `evidence`, `helpers`, `errors` и `constants`; `backend/src/domains/graph/projection.rs` сокращен с 708 до 15 строк и оставлен как public facade, все новые graph projection modules меньше 700 строк.
* [x] По текущему scan устранены backend source files больше 700 строк: максимальный backend `.rs` файл — `backend/src/domains/mail/handlers/workflow_actions.rs` на 688 строк.
* [x] Mail workflow actions boundary разделен на bounded modules `handler`, `models`, `source`, `response`, `validation`, `constants` и action modules `reply`, `tasks`, `documents`, `calendar`, `persons`, `archive`; `backend/src/domains/mail/handlers/workflow_actions.rs` сокращен с 688 до 9 строк и оставлен как crate-local route facade.
* [x] Mail message store boundary разделен на bounded modules `queries`, `upsert`, `workflow`, `local_state` и `metadata`; `backend/src/domains/mail/messages/store.rs` сокращен с 667 до 18 строк и оставлен как `MessageProjectionStore` facade, при этом projection persistence, read queries, workflow state, local trash и metadata writes больше не смешаны в одном файле.
* [x] App error conversions boundary разделен на bounded modules `platform`, `ai`, `integrations`, `knowledge`, `mail`, `persons`, `organizations`, `calendar`, `tasks` и `documents`; `backend/src/app/error/conversions.rs` сокращен с 657 до 10 строк и оставлен как module facade для `From<T> for ApiError`.
* [x] Email sync pipeline boundary разделен на bounded modules `service`, `raw_records`, `attachments`, `knowledge`, `participants`, `relationships`, `organizations`, `candidates`, `report` и `errors`; `backend/src/workflows/email_sync_pipeline.rs` сокращен с 654 до 14 строк и оставлен как public workflow facade, при этом provider batch orchestration, raw/blob projection, attachment scan persistence, Persona projection, relationship events, organization links и candidate refresh больше не смешаны в одном файле.
* [x] Persons core compatibility boundary разделен на bounded modules `identities`, `roles`, `interaction_contexts`, `preferences` и `errors`; `backend/src/domains/persons/core.rs` сокращен с 650 до 10 строк и оставлен как public facade, при этом identity traces, role-to-Relationship materialization и deprecated `person_personas` interaction-context-to-preferences adapter больше не смешаны в одном файле.
* [x] Automation Engine boundary разделен на bounded modules `store`, `dry_run`, `policy`, `models`, `rows`, `validation`, `errors`, `ids` и `constants`; `backend/src/engines/automation.rs` сокращен с 631 до 16 строк и оставлен как public facade, при этом automation template/policy persistence, Telegram dry-run event/outbound write, scoped policy evaluation, DTO, SQL row mapping и validation helpers больше не смешаны в одном файле.
* [x] AI Core service boundary разделен на bounded modules `core`, `status`, `answer`, `task_candidates`, `task_candidate_persistence`, `meeting_prep`, `retrieval`, `attribution`, `events` и `model_config`; `backend/src/ai/core/service.rs` сокращен с 616 до 12 строк и оставлен как public `AiService` facade, при этом answer orchestration, task candidate refresh, meeting prep, source-backed retrieval, Persona attribution, canonical AI run events и model provenance больше не смешаны в одном файле.
* [x] Platform audit boundary разделен на bounded modules `store`, `models`, `events`, `reviews`, `communication`, `telegram`, `documents`, `settings`, `helpers`, `errors` и `constants`; `backend/src/platform/audit.rs` сокращен с 611 до 15 строк и оставлен как public facade, при этом append-only audit persistence, event audit read-model, domain review audit records, provider-write metadata, Telegram capability audit metadata и error mapping больше не смешаны в одном файле.
* [x] Mail sending/actions handler boundary разделен на bounded modules `provider_send`, `local_state`, `certificates`, `forwarding`, `multilingual`, `ai_reply` и `extraction`; `backend/src/domains/mail/handlers/sending.rs` сокращен с 605 до 21 строки и оставлен как crate-local route facade, при этом SMTP provider-write orchestration, credential lookup, audit logging, local trash/read aliases, certificate/signature endpoints, reply/forward actions, translation, AI reply и extraction endpoints больше не смешаны в одном файле.
* [x] Telegram TDLib JSON facade очищен от встроенной unit-test спецификации: `backend/src/integrations/telegram/tdjson.rs` сокращен с 603 до 40 строк, тесты перенесены в `backend/src/integrations/telegram/tdjson/tests.rs`, production TDLib client lifecycle, request/response parsing, QR login state и file download helpers не менялись.
* [x] Persons API projection boundary разделен на bounded modules `store`, `models`, `rows`, `validation`, `participants` и `errors`; `backend/src/domains/persons/api.rs` сокращен с 596 до 11 строк и оставлен как public facade, при этом Persona DTO/type contracts, compatibility `persons` storage, row mapping, participant normalization, Owner Persona writes и Graph projection updates больше не смешаны в одном файле. ADR-0084 compatibility contract для `/persons` и `person_id` сохранен до отдельной schema/API migration ADR.
* [x] Mail account setup handler boundary разделен на bounded modules `gmail_oauth`, `gmail_callback`, `imap`, `calendar`, `models` и `helpers`; `backend/src/domains/mail/handlers/account_setup.rs` сокращен с 591 до 10 строк и оставлен как crate-local route facade, при этом Gmail OAuth lifecycle, callback HTML, IMAP/iCloud setup, host-vault gate, calendar account coupling, provider DTO validation и setup response mapping больше не смешаны в одном файле.
* [x] Calendar events/account/source boundary разделен на bounded modules `accounts`, `sources`, `event_store`, `models`, `queries`, `rows` и `errors`; `backend/src/domains/calendar/events.rs` сокращен с 569 до 17 строк и оставлен как public facade, при этом calendar account metadata/upsert, source projections, event CRUD/list/reschedule, recurrence/timezone DTOs и row/error mapping больше не смешаны в одном файле. ADR-0021/0067/0068 source/event identity и provider credential reference boundary сохранены без новых compatibility layers.
* [x] Memory Engine boundary разделен на bounded modules `cards`, `facts`, `context`, `gaps`, `stale`, `cross_domain`, `models`, `validation` и `errors`; `backend/src/engines/memory.rs` сокращен с 567 до 92 строк и оставлен как public facade, при этом compatibility notes-to-card assembly, source-backed Persona fact normalization, entity context packs, required fact gap detection, stale-memory review candidates, cross-domain ranking/citations, DTO и validation/error mapping больше не смешаны в одном файле. Memory Engine semantics из `docs/engines/memory.md`, ADR-0057 и ADR-0084 сохранены: output remains source-backed, reviewable and derived, not AI/source-of-truth mutation.
* [x] Task candidate store boundary разделен на bounded modules `list`, `refresh`, `review`, `task_activation` и `obligation_sync`; `backend/src/domains/tasks/candidates/store.rs` сокращен с 546 до 57 строк и оставлен как `TaskCandidateStore` facade, при этом deterministic message/document refresh, legacy task fragment extraction, Obligation Engine candidate wiring, review event append/replay, task activation, obligation-derived review-state sync и list query больше не смешаны в одном файле. ADR-0020/0070/0071/0088 semantics сохранены: candidates remain review-first, idempotent by source evidence/title, no automatic task activation without review, and source evidence/provenance remains explicit.
* [x] Contradiction observation store boundary разделен на bounded modules `observations`, `refresh`, `review` и `sources`; `backend/src/engines/consistency/store.rs` сокращен с 545 до 59 строк и оставлен как `ContradictionObservationStore` facade, при этом deterministic Polygraph refresh orchestration, source evidence queries, observation upsert/list persistence и review-state update больше не смешаны в одном файле. ADR-0085/0087 semantics сохранены: observations remain source-backed and reviewable, refresh does not overwrite Memory, Trust, Risk or Relationships, and owner review remains explicit.
* [x] Persona memory compatibility boundary разделен на bounded modules `facts`, `cards`, `preferences`, `relationship_events`, `snapshots` и `errors`; `backend/src/domains/persons/memory.rs` сокращен с 532 до 13 строк и оставлен как public facade, при этом source-backed fact normalization through Memory Engine, memory-card persistence, Persona Preferences, relationship timeline events, snapshot history diff и memory decay projection больше не смешаны в одном файле. ADR-0057/0084/0090 semantics сохранены: compatibility `person_*` storage remains explicit until a schema/API migration ADR, facts/cards/preferences keep provenance, confidence and reviewable-memory boundaries, and AI output is still normalized before durable writes.
* [x] Persona investigator / Dossier boundary разделен на bounded modules `assembly`, `snapshots`, `meeting_prep`, `sections`, `models`, `service` и `errors`; `backend/src/domains/persons/investigator.rs` сокращен с 530 до 13 строк и оставлен как public facade, при этом Dossier read-model assembly, persisted snapshot cache/review, meeting preparation, source-backed section builders, DTO и error mapping больше не смешаны в одном файле. ADR-0084 semantics сохранены: Dossier remains a generated cited read model with summary, interests, projects, organizations, skills, communication patterns, AI observations, source refs and legacy compatibility fields.
* [x] Timeline Engine boundary разделен на bounded modules `policy`, `summaries`, `signals`, `cross_domain`, `replay`, `projection`, `models`, `validation` и `errors`; `backend/src/engines/timeline.rs` сокращен с 525 до 114 строк и оставлен как public facade, при этом bounded timeline policy, source-backed event validation, period summaries, recency/gap/snapshot diff signals, cross-domain timeline assembly, canonical event-log replay mapping и cursor-backed projection runner больше не смешаны в одном файле. ADR-0001/0023/0034/0036 и `docs/engines/timeline.md` сохранены: Timeline remains a derived view, replay is cursor-backed, and durable read-model storage remains a future schema/API decision.
* [x] Telegram API route boundary разделен на bounded modules `accounts`, `capabilities`, `runtime`, `qr_login`, `chats`, `messages`, `media` и `helpers`; `backend/src/integrations/telegram/api.rs` сокращен с 521 до 22 строк и оставлен как route-handler facade, при этом account lifecycle, capability contract, TDLib runtime start/status, QR login, chat/history sync, fixture/manual messages, media downloads, secret-store helper и audit actor policy больше не смешаны в одном файле. ADR-0050/0083/0091 semantics сохранены, а ADR-0056 actor rule исправлен: Telegram audit records now use constant `hermes-frontend` and ignore legacy `x-hermes-actor-id`.
* [x] Telegram client model boundary разделен на bounded modules `accounts`, `qr_login`, `chats` и `messages`; `backend/src/integrations/telegram/client/models.rs` сокращен с 520 до 21 строки и оставлен как DTO facade, при этом account setup/live setup, credential binding response models, QR login DTO, chat sync state, message ingest DTO, TDLib media-message empty-text rule, provider-write manual-send DTO, delivery states и attachment anchor больше не смешаны в одном файле. ADR-0050/0083/0091 и `docs/telegram/` semantics сохранены: provider kinds stay account-scoped, secret bindings remain references only, QR/live account lifecycle is unchanged, and delivery/manual-send validation remains in the Telegram client boundary.
* [x] Task core boundary разделен на bounded modules `providers`, `external_identities`, `context_packs`, `evidence`, `relations`, `checklists`, `subtasks` и `errors`; `backend/src/domains/tasks/core.rs` сокращен с 516 до 17 строк и оставлен как public facade, при этом provider account records, external provider identities, local context overlay, source evidence, compatibility task relations with first-class Relationship projection, checklist snapshots и subtask links больше не смешаны в одном файле. ADR-0020/0070/0071/0088 и `docs/domains/tasks.md` semantics сохранены: task candidates remain review-first, provider sync identity stays separate from local context, evidence/provenance remains explicit, and Obligations/Decisions are not automatically collapsed into Tasks.
* [x] Mail sync boundary разделен на bounded modules `models`, `planning`, `recording`, `raw_payload`, `ids`, `validation` и `errors`; `backend/src/domains/mail/sync.rs` сокращен с 516 до 15 строк и оставлен как public facade, при этом provider sync planning, account-scoped credential purpose selection, delimiter-safe IMAP stream IDs, raw provider record persistence, checkpoint writes, raw `.eml` blob projection, raw payload stripping, base64 decoding, ID encoding и error mapping больше не смешаны в одном файле. ADR-0041/0046/0055 и `docs/mail/architecture.md` semantics сохранены: raw provider records remain append-only/idempotent, checkpoints stay per account and stream, heavy raw mail bytes stay in local blob storage with metadata references in PostgreSQL, secret-like config is rejected, and automated tests keep read-only provider paths.
* [x] AI semantic retrieval boundary разделен на bounded modules `models`, `store`, `embeddings`, `search`, `indexing`, `sources`, `rows` и source extraction modules for messages/documents/projects/tasks/personas; `backend/src/ai/core/semantic.rs` сокращен с 500 до 18 строк и оставлен как public facade, при этом embedding upsert/freshness, vector search, text fallback search, canonical source extraction, row mapping, index orchestration и DTO больше не смешаны в одном файле. ADR-0007/0049/0081/0082 и `docs/engines/search.md` semantics сохранены: embeddings remain derived/rebuildable, halfvec(2560) dimension validation is unchanged, source-backed retrieval keeps canonical source provenance, AI output is not source of truth, and `Person` continues to use storage-compatible `contact` source_kind until a dedicated schema/API migration ADR.
* [x] Telegram runtime manager boundary разделен на bounded modules `account`, `actor_states`, `registry`, `lifecycle`, `tdlib_actor`, `sync_chats`, `sync_history`, `sync_history_tdlib`, `send` и `media_download`; `backend/src/integrations/telegram/runtime/manager.rs` сокращен с 490 до 40 строк и оставлен как runtime-manager facade/type owner, при этом account active gate, in-memory actor registry, status/start lifecycle, TDLib actor bootstrap, chat sync, selected history sync, manual provider-write send и media download orchestration больше не смешаны в одном файле. ADR-0083/0091 и `docs/telegram/` semantics сохранены: один actor на account, fixture runtime остается доступным для тестов, TDLib commands остаются account-scoped, live send/media paths stay capability-gated, raw provider evidence/projections remain local-first, and downloaded media still passes through the communication attachment/blob safety-scanner boundary.
* [x] Mail background sync store boundary разделен на bounded modules `account`, `settings`, `statuses`, `scheduling`, `orphaned`, `run_start`, `run_progress`, `run_finish` и `run_latest`; `backend/src/domains/mail/background_sync/store.rs` сокращен с 481 до 22 строк и оставлен как `MailSyncStore` facade/type owner, при этом account existence gate, account-scoped settings defaults/updates, sanitized sync status view, due-account scheduling, backend-restart recovery, durable run start/progress/recoverable-resync/finish/latest-response persistence больше не смешаны в одном файле. ADR-0041/0046/0055/0080 semantics сохранены: supported provider kinds stay `gmail`/`icloud`/`imap`, checkpoints remain account/stream scoped, run status stores sanitized metadata only, raw payloads/blob bytes/secrets stay outside sync status records, and local-first background sync remains account-scoped without overlapping active runs.
* [x] Telegram TDLib QR login boundary разделен на bounded modules `commands`, `driver`, `worker`, `worker_state`, `authorization` и `tdlib_commands`; `backend/src/integrations/telegram/tdjson/qr_login.rs` сокращен с 478 до 10 строк и оставлен как QR-login facade, при этом public API entrypoints, password/cancel pending-session mutations, same-account setup cancellation, worker spawn, TDLib command draining, setup/encryption events, QR link issuance, 2FA password state, ready/failed/expired transitions и TDLib close handling больше не смешаны в одном файле. ADR-0083/0091 и `docs/telegram/` semantics сохранены: QR authorization remains account-scoped, TDLib local state path stays account-specific, password values are command-only and not logged/audited, setup cancellation notifies the worker, and live capability remains blocked unless runtime dependencies and validation gates exist.
* [x] Organization core boundary разделен на bounded modules `identity`, `aliases`, `domains`, `departments`, `contact_links`, `related` и `errors`; `backend/src/domains/organizations/core.rs` сокращен с 475 до 15 строк и оставлен как public facade, при этом Organization identity persistence, aliases, domains, department hierarchy, Persona-Organization contact links, related organizations, error mapping и Relationship projection больше не смешаны в одном файле. ADR-0061/0062/0066 и `docs/domains/organizations.md` semantics сохранены: Organization остается first-class domain entity, identity/domain/alias records keep provenance, `organization_contact_links` continues to materialize source-backed `member_of` Relationships, and graph traversal remains derived from Relationship/graph infrastructure rather than new storage.
* [x] AI API handler boundary разделен на bounded modules `runtime`, `control_center`, `models` и `helpers`; `backend/src/ai/api.rs` сокращен с 473 до 15 строк и оставлен как route-handler facade, при этом AI status/agents/runs, source-backed answer generation, task-candidate refresh, meeting prep, AI Settings Control Center providers/models/routes/prompts, host-vault API key storage и actor-id helper больше не смешаны в одном файле. ADR-0007/0049/0081/0082 и `docs/engines/search.md` semantics сохранены: embeddings remain derived/rebuildable, answer/task/meeting workflows continue through source-backed retrieval and persisted `ai_agent_runs`, Ollama remains default, OmniRoute/remote providers stay opt-in/consent-gated, provider secrets remain in host vault, and prompt/audit/event metadata still avoids raw private source text or secret payloads.
* [x] Platform call intelligence boundary разделен на bounded modules `store`, `models`, `rows`, `stt`, `validation` и `errors`; `backend/src/platform/calls.rs` сокращен с 472 до 14 строк и оставлен как public facade, при этом Telegram call metadata persistence, call transcript persistence, fixture speech-to-text provider, row mapping, request validation и error mapping больше не смешаны в одном файле. ADR-0050/0083/0091 и `docs/telegram/` semantics сохранены: current implementation stores call metadata and fixture transcripts, live call control/audio capture/real STT remain blocked runtime slices, hidden recording remains unsupported, transcripts remain local/policy-account-chat scoped, and successful call transcript text continues to feed Polygraph evidence without overwriting Memory.
* [x] Mail provider sync orchestration boundary разделен на bounded modules `gmail`, `gmail/history`, `gmail/message_list`, `imap`, `projection`, `summary` и `types`; `backend/src/domains/mail/background_sync/provider.rs` сокращен с 454 до 43 строк и оставлен как provider-adapter facade, при этом Gmail OAuth/history/list paging, IMAP credential lookup and UID-validity recovery, provider batch projection/blob handoff/graph refresh, run summary aggregation и context/config types больше не смешаны в одном файле. ADR-0041/0046/0055/0080 semantics сохранены: Gmail/iCloud/IMAP accounts stay supported, credential lookup remains account-scoped and runtime-only, automated tests keep read-only provider paths, run status stores sanitized metadata, checkpoints remain account/stream scoped, and raw mail bytes continue through local blob storage instead of PostgreSQL payload/status records.
* [x] Platform event spine boundary разделен на bounded modules `store`, `store/append`, `store/read`, `store/replay`, `cursors`, `models`, `builder`, `rows`, `migrations`, `validation` и `errors`; `backend/src/platform/events.rs` сокращен с 446 до 15 строк и оставлен как public facade, при этом canonical event append/transaction append, event lookup, replay by durable position, projection cursor persistence, envelope DTO/builder validation, row mapping, backend-managed migrations и error mapping больше не смешаны в одном файле. ADR-0001/0033/0034/0036 semantics сохранены: `event_log.position` остается replay order, event envelopes stay versioned and source-backed, event log remains append-only through storage constraints, cursor saves remain monotonic, migrations are backend-managed, and projection runners advance cursors only after successful handlers.
* [x] Mail account setup service boundary разделен на bounded modules `constructors`, `gmail_start`, `gmail_complete`, `gmail_refresh`, `gmail_payloads`, `imap`, `imap_payloads`, `stores` и `token_http`; `backend/src/domains/mail/accounts/service.rs` сокращен с 439 до 25 строк и оставлен как `EmailAccountSetupService` type facade, при этом service construction, Gmail OAuth PKCE URL creation, OAuth completion and token bundle persistence, Gmail access-token refresh, IMAP/iCloud account setup, SMTP/IMAP non-secret config payload building, account-scoped store accessors and provider token HTTP calls больше не смешаны в одном файле. ADR-0041/0042/0055/0076 semantics сохранены: Gmail/iCloud/IMAP account setup keeps provider config non-secret, credentials remain account-scoped by `account_id` plus purpose, SMTP gets a separate `smtp_password` binding, new runtime setup writes host-vault references through the service factory, legacy database encrypted vault remains test/migration compatibility, and provider account config/secret metadata continue to exclude plaintext OAuth tokens and mailbox/app passwords.
* [x] Telegram TDLib actor boundary разделен на bounded modules `spawn`, `start_request`, `session`, `driver`, `authorization`, `chats`, `history`, `send`, `download`, `responses` и `support`; `backend/src/integrations/telegram/runtime/actor.rs` сокращен с 437 до 15 строк и оставлен как actor facade, при этом TDLib runtime spawn, account-scoped start request, session-key credential lookup, serialized command loop, authorization bootstrap, chat/history/send/download commands, correlated responses and cursor helpers больше не смешаны в одном файле. ADR-0083/0091 semantics сохранены: один TDLib actor остается account-scoped runtime boundary, fixture runtime не затронут, command serialization is unchanged, credential lookup remains `account_id` + purpose, provider writes stay manager/policy-gated, and message/media bytes are not introduced into audit records.
* [x] Mail fixture export boundary разделен на bounded modules `raw_payload`, `rfc822`, `headers`, `body`, `encoding`, `encoded_words`, `redaction`, `models`, `errors` и `text`; `backend/src/domains/mail/fixtures/export.rs` сокращен с 434 до 51 строки и оставлен как public export facade, при этом sync-batch fixture orchestration, raw RFC822 base64 extraction, header/body parsing, multipart/text/html body normalization, transfer/RFC2047 decoding, privacy redaction and error mapping больше не смешаны в одном файле. ADR-0041/0046/0055 semantics сохранены: fixture export continues from provider raw payload evidence, outputs redacted attachment-free fixtures, does not expose secrets/private message text, and automated fixture tests remain read-only.
* [x] AppConfig boundary разделен на bounded modules `defaults`, `env`, `core_env`, `provider_env`, `ai_env` и `accessors`; `backend/src/platform/config/app_config.rs` сокращен с 430 до 43 строк и оставлен как `AppConfig` model facade, при этом local-first defaults, router-level local API secret, legacy database-vault compatibility env, host-vault dev paths, TDLib/Telegram/Google provider settings, AI runtime settings and accessor fallbacks больше не смешаны в одном файле. ADR-0032/0056/0076 semantics сохранены: Docker config remains development-only, protected local APIs still use `HERMES_LOCAL_API_SECRET`, host-vault paths remain runtime config, and `HERMES_SECRET_VAULT_KEY` stays legacy compatibility rather than new provider credential storage.
* [x] App error response boundary разделен на bounded modules `platform`, `knowledge`, `review`, `communication`, `persons`, `mail`, `document_processing`, `ai/runtime`, `ai/control_center`, `integrations/telegram`, `integrations/whatsapp`, `integrations/automation` и `integrations/call`; `backend/src/app/error/response.rs` сокращен с 429 до 107 строк и оставлен как `IntoResponse` adapter, при этом JSON envelope assembly, local API auth header behavior, platform/store errors, graph/project errors, review-engine errors, communication/mail errors, Persona errors, document processing errors, AI errors and integration errors больше не смешаны в одном файле. ADR-0056 semantics сохранены: router-level `X-Hermes-Secret` auth behavior remains outside handlers, API response JSON codes/statuses remain stable, and audit actor logic is not reintroduced into response mapping.
* [x] Person identity store boundary разделен на bounded modules `candidates`, `name_merge_candidates`, `split_candidates`, `review`, `review_state` и `queries`; `backend/src/domains/persons/identity/store.rs` сокращен с 428 до 23 строк и оставлен как `PersonIdentityStore` facade/type owner, при этом conservative candidate generation, split-candidate materialization, owner-review event append/replay, review-state projection and identity read queries больше не смешаны в одном файле. ADR-0074/0084/0090 semantics сохранены: ambiguous Persona identity resolution remains reviewable, confirmed/rejected decisions remain event-backed, `persons`/`person_id` storage stays an explicit compatibility boundary until a migration ADR, and no automatic merge or schema rename is introduced.
* [x] Person projection store boundary разделен на bounded modules `email_projection`, `persona_reads`, `persona_writes`, `owner`, `persona_type` и `ai_agents`; `backend/src/domains/persons/api/store.rs` сокращен с 423 до 23 строк и оставлен как `PersonProjectionStore` facade/type owner, при этом email-backed compatibility projection, Persona-native bridge reads, narrow ADR-0090 writes, single Owner Persona assignment, PersonaType mutation and AI agent Persona materialization больше не смешаны в одном файле. ADR-0084/0090 semantics сохранены: `/api/v1/personas/*` remains a narrow compatibility bridge over `persons`, `is_self=false` behavior stays in the handler boundary, physical table/API rename is not introduced without migration ADR, and AI agent Personas still materialize as `ai_agent` with graph node projection.
* [x] Email Intelligence workflow boundary разделен на bounded modules `models`, `categories`, `service`, `heuristics`, `prompt`, `errors` и `tests`; `backend/src/workflows/email_intelligence.rs` сокращен с 410 до 14 строк и оставлен как public workflow facade, при этом DTO/category contract, local AI prompt/response parsing, derived analysis persistence, heuristic scoring/category rules and tests больше не смешаны в одном файле. ADR-0046/0049/0082 semantics сохранены: mail intelligence remains derived state over message projections, raw mail/blob and attachment safety boundaries are not crossed, AI output remains non-authoritative, prompt version/text and heuristic scoring behavior stay unchanged.
* [x] AI Control Center prompt boundary разделен на bounded modules `templates`, `versions`, `activation`, `evaluation`, `eval_runs` и `lookups`; `backend/src/ai/control_center/prompts.rs` сокращен с 410 до 6 строк и оставлен как prompt facade, при этом prompt template CRUD, version creation, active-version transition, prompt studio preview/eval persistence, eval-run listing и internal prompt/version lookup больше не смешаны в одном файле. ADR-0082 semantics сохранены: prompt templates remain versioned, system prompts stay read-only, editable user prompts continue as domain records, prompt evaluation persists output/metadata, and secret/private-source-text rejection boundaries remain unchanged.
* [x] Platform settings definitions boundary разделен на registry facade, category modules `server`, `frontend`, `ai`, `ui` и focused nested modules для frontend/AI declarations; `backend/src/platform/settings/definitions.rs` сокращен с 404 до 30 строк и оставлен как declared-settings registry facade, при этом server bootstrap setting, frontend API/layout/sidebar/theme/locale/UI-state declarations, AI provider/model/runtime declarations, UI shell declarations и settings unit tests больше не смешаны в одном файле. ADR-0054 semantics сохранены: application settings remain declared-only, typed, non-secret, provider credentials stay outside settings, frontend locale/UI-state contracts and private-content rejection remain unchanged, and repair/list/update store callers continue через тот же `declared_application_settings` API.
* [x] Telegram TDLib QR-login support boundary разделен на focused modules `authorization`, `completion`, `constants`, `identity`, `identifiers`, `pending`, `qr`, `responses` и `types`; `backend/src/integrations/telegram/tdjson/qr_login_support.rs` сокращен с 402 до 26 строк и оставлен как QR support facade/re-export boundary, при этом session/command types, worker completion, status response builders, QR SVG rendering, TDLib user identity parsing, pending-session mutations, authorization-state helpers and setup/thread identifiers больше не смешаны в одном файле. ADR-0083/0091 semantics сохранены: QR authorization remains account-scoped, stale QR tokens stay hidden on password/ready states, cancel/password commands remain worker commands, TDLib session paths remain account-scoped through existing callers, and helper visibility is constrained to the `tdjson` boundary rather than widened crate-wide.
* [x] Shared API query parsing boundary разделен на focused modules `communication`, `documents`, `graph`, `persons`, `projects` и `tasks`; `backend/src/domains/api_support/query_parsing.rs` сокращен с 394 до 13 строк и оставлен как crate-local facade/re-export boundary, при этом communication message filters, document processing job limits/validation, graph picker/search/neighborhood query parsing, person identity review query validation, project/project-link review parsing и task-candidate review parsing больше не смешаны в одном файле. ADR-0073 semantics сохранены: route handlers continue to import through the shared API support facade, API-layer parsing stays outside domain persistence/services, and existing query parameter names, clamps and error messages remain unchanged.
* [x] Mail signature/certificate boundary разделен на focused modules `certificate_type`, `provider`, `storage_kind`, `trust`, `models`, `detector`, `store`, `rows`, `errors` и `tests`; `backend/src/domains/mail/signatures.rs` сокращен с 388 до 20 строк и оставлен как public facade/re-export boundary, при этом certificate enum string contracts, certificate DTOs, signature detection, expiry warning logic, SQL store operations, row mapping, validation/error mapping и unit tests больше не смешаны в одном файле. ADR-0073, ADR-0046 и Communications semantics сохранены: mail signatures remain channel-specific Communications helpers, route callers keep the same `crate::domains::mail::signatures::*` API, certificate rows still store metadata only, and signature detection behavior/error contracts remain unchanged.
* [x] Calendar Intelligence heuristic boundary разделен на focused modules `classification`, `fingerprint`, `scoring`, `conference`, `location`, `analytics`, `models` и `errors`; `backend/src/domains/calendar/intelligence.rs` сокращен с 384 до 13 строк и оставлен как pure service facade/type owner, при этом event classification, importance/readiness scoring, risk detection, heuristic fingerprinting, conference-link extraction, location parsing, time categorization, back-to-back grouping, DTOs и error type больше не смешаны в одном файле. ADR-0069 semantics сохранены: `CalendarIntelligenceService` остается stateless deterministic heuristic service without DB/provider dependencies, while Calendar Brain/context-pack behavior remains outside this pure helper boundary.
* [x] Calendar meetings boundary разделен на focused modules `models`, `errors`, `rows`, `notes`, `outcomes`, `outcome_projection`, `recordings` и `transcripts`; `backend/src/domains/calendar/meetings.rs` сокращен с 381 до 15 строк и оставлен как public facade/re-export boundary, при этом meeting notes, meeting outcomes, explicit Decision/Obligation outcome projection, event recordings, transcripts, row mapping и error mapping больше не смешаны в одном файле. ADR-0068/0088/0089 и `docs/workflows/meeting-to-decisions.md` semantics сохранены: meeting outcome decisions become source-backed suggested Decisions impacted by Event, promise/task/follow-up outcomes become source-backed suggested Obligations, Tasks/Projects are not auto-created, and `linked_entity_id` is retained after accepted projection.
* [x] Host vault reconciliation boundary разделен на focused modules `lifecycle`, `service`, `manifest_enrichment`, `provider_recovery`, `calendar_restore`, `metadata`, `summary` и `errors`; `backend/src/app/vault_reconciliation.rs` сокращен с 377 до 10 строк и оставлен как crate-local facade/re-export boundary, при этом startup spawn gate, host-vault manifest enrichment from PostgreSQL, provider account recovery, account-to-secret binding repair, linked calendar account repair, fallback metadata и error mapping больше не смешаны в одном файле. ADR-0076, ADR-0054 и storage/security architecture semantics сохранены: reconciliation remains idempotent and non-secret, PostgreSQL receives only account metadata, `secret_references` and account-to-secret bindings, host vault remains payload source of truth, and logs do not expose credential values.
* [x] Calendar event API handler boundary разделен на focused modules `crud`, `status`, `participants`, `relations`, `context_pack`, `agenda` и `checklist`; `backend/src/domains/calendar/handlers/events.rs` сокращен с 371 до 15 строк и оставлен как route-handler facade/re-export boundary, при этом event list/create/read/update/delete, reschedule/cancel mutations, participant endpoints, graph relation endpoints, context-pack materialization, agenda persistence и checklist persistence больше не смешаны в одном файле. ADR-0021/0067/0068 и `docs/domains/calendar-events.md` semantics сохранены: Calendar remains first-class event source/domain owner, events keep source identity and query filters, participants/relations stay explicit graph/context links, and Calendar handler composition remains separate from Calendar Intelligence and meeting workflows.
* [x] Telegram client message boundary разделен на focused modules `ingestion`, `tdlib_ingestion`, `manual_send`, `queries`, `attachments`, `account_lookup`, `chat_lookup` и `intelligence`; `backend/src/integrations/telegram/client/messages.rs` сокращен с 370 до 8 строк и оставлен как client-message facade, при этом fixture/TDLib message ingestion, raw source record projection, Decision/Task candidate refresh, manual fixture send response assembly, recent-message query, attachment anchors and account/chat lookup больше не смешаны в одном файле. ADR-0083/0091 и `docs/telegram/` semantics сохранены: Telegram messages remain append-only source evidence projected into Communications, manual sends remain provider-write actions with redacted audit handled at API/runtime boundary, TDLib media messages may keep empty text, and attachment anchors still route through the communication attachment/blob scanner boundary.
* [x] Calendar core event context boundary разделен на focused modules `participants`, `relations`, `context_packs`, `agendas`, `checklists` и `errors`; `backend/src/domains/calendar/core.rs` сокращен с 368 до 13 строк и оставлен как public facade/re-export boundary, при этом participant persistence, graph relation persistence, event context-pack materialization, agenda persistence, checklist persistence и error mapping больше не смешаны в одном файле. ADR-0067/0068 и `docs/domains/calendar-events.md` semantics сохранены: Calendar remains first-class event domain, participants and event relations stay explicit graph/context links, context packs remain derived JSON snapshots, and event identity storage remains outside Calendar core context stores.
* [x] Mail background sync model boundary разделен на focused modules `settings`, `status`, `runs`, `progress`, `finish` и `failures`; `backend/src/domains/mail/background_sync/models.rs` сокращен с 361 до 17 строк и оставлен как facade/re-export boundary, при этом public sync settings/status/run DTOs, durable run response conversion, internal trigger/phase/progress state, finish-run persistence command и sanitized provider/vault failure mapping больше не смешаны в одном файле. ADR-0041/0046/0055/0080 semantics сохранены: sync status records expose only sanitized metadata and checkpoint presence, provider credentials remain account-scoped runtime-only, raw mail/blob bytes stay outside PostgreSQL status records, and local-first account sync behavior remains unchanged.
* [x] Telegram TDLib parsing boundary разделен на focused modules `chats`, `messages`, `message_parts`, `files`, `events` и `values`; `backend/src/integrations/telegram/tdjson/parsing.rs` сокращен с 359 до 14 строк и оставлен как crate-local facade/re-export boundary, при этом chat-id/chat snapshot parsing, message list/snapshot parsing, sender/text extraction, file snapshot parsing, authorization/bootstrap error event parsing и низкоуровневые TDLib value helpers больше не смешаны в одном файле. ADR-0083/0091 и `docs/telegram/` semantics сохранены: TDLib parsing remains a provider adapter/source-evidence boundary, QR/runtime request handling stays outside parser modules, media messages may keep empty text, and parser errors do not introduce message bodies, media bytes or secrets into audit/logging state.
* [x] Проведен дополнительный code review AI Control Center provider/model routing boundary относительно ADR-0082/0081/0076: подтверждено, что текущее поведение сохраняет host-vault запись секретов, но backend не enforce-ит готовность remote/API provider перед route/prompt selection.
* [x] AI Control Center provider readiness invariant приведен к ADR-0082/0076: route selection, prompt evaluation и runtime route resolution теперь используют общий `availability` guard, remote/API provider недоступен для private-context workflows без `consent_state = granted`, `status = ready` и host-vault `api_token` binding в `ai_provider_secret_refs`, `provider_command(Test)` больше не возвращает `ok` по consent alone, а Settings UI отключает remote model options до consent/setup completion. Добавлены Testcontainers-backed regression tests для missing/valid host-vault binding и frontend unit coverage для model selector availability.
* [x] AI Control Center provider boundary разделен на focused modules `queries`, `create`, `update`, `commands`, `consent` и `secrets`; `backend/src/ai/control_center/providers.rs` сокращен с 377 до 6 строк и оставлен как facade, все provider modules меньше 120 строк. ADR-0082 readiness guard не ослаблен: route/prompt/runtime paths продолжают использовать общий availability boundary. Дополнительно устранен Testcontainers readiness flake в `crates/testkit/src/containers/postgres.rs` заменой fixed sleep на bounded connect retry.
* [x] Проведен дополнительный code review текущего AI Control Center diff и дополнен `frontend/docs/code-review-2026-06-14.md`: выявлены follow-up issues вокруг запрета `api_key` для не-API providers, partial create при host-vault failure и consent mutation для providers, которым consent не нужен.
* [x] Закрыты follow-up issues из AI Control Center review: `api_key` теперь разрешен только для `api` providers на create/update и secret-binding boundaries; create/patch с `api_key` выполняют host-vault unlock preflight до provider mutation, поэтому locked/uninitialized vault не оставляет partial provider row; `record_consent` теперь API-only и не меняет `not_required` consent state у built-in/CLI providers. Добавлены Testcontainers-backed regression tests на все три сценария.
* [x] OmniRoute runtime client boundary разделен на focused modules `config`, `catalog`, `chat`, `embeddings`, `transport`, `models` и `error`; `backend/src/integrations/omniroute/client.rs` сокращен с 344 до 73 строк и оставлен как public facade/constructor boundary, все новые Omniroute modules меньше 120 строк. ADR-0081/0082 semantics сохранены: Ollama остается default provider, OmniRoute remains explicit opt-in, OpenAI-compatible `/models`, `/chat/completions` и `/embeddings` paths, bearer auth, env-backed API key boundary, thinking-tag stripping and 2560-dimensional embedding behavior stay unchanged.
* [x] Project read-model boundary разделен на focused modules `projects`, `keywords`, `stats`, `messages`, `documents`, `people`, `timeline` и `reviewed_targets`; `backend/src/domains/projects/core/read_model.rs` сокращен с 343 до 8 строк и оставлен как read-model facade, все новые Project read-model modules меньше 120 строк. ADR-0047/0048 и `docs/domains/projects.md` semantics сохранены: project detail remains metadata + stats + recent communications + related documents + key people + timeline from active reviewed links, rejected keyword links stay excluded, confirmed non-keyword links remain included, and message body text is not exposed by the read model.
* [x] Проведен дополнительный code review текущих OmniRoute и Project SRP split changes и дополнен `frontend/docs/code-review-2026-06-14.md`: code-level regressions внутри split modules не подтверждены, но зафиксированы открытые review risks вокруг untracked split modules и недостаточного default coverage для Project read-model live SQL behavior.
* [x] Mail core model boundary разделен на focused modules `provider_kind`, `accounts`, `raw_records`, `checkpoints` и `secrets`; `backend/src/domains/mail/core/models.rs` сокращен с 342 до 16 строк и оставлен как facade/re-export boundary, все новые Mail core model modules меньше 120 строк. ADR-0041/0046/0055/0076 semantics сохранены: provider account config remains non-secret JSON metadata, raw communication records remain append-only source evidence, ingestion checkpoints stay account/stream scoped, and provider credential bindings still enforce account-scoped secret purpose to compatible secret kind.
* [x] Persons enrichment boundary разделен на focused modules `commands`, `queries`, `materialization`, `relationship_materialization`, `rows`, `models`, `errors` и `store`; `backend/src/domains/persons/enrichment.rs` сокращен с 340 до 12 строк и оставлен как facade/re-export boundary, все новые enrichment modules меньше 120 строк. ADR-0084/0086/0090 semantics сохранены: compatibility `persons` projection remains Persona-compatible, notes/favorite materialize Persona memory/preference records, and `trust_score` still materializes a suggested Owner Persona relationship with evidence instead of overwriting memory.
* [x] Shared API support stores boundary разделен на focused modules `domain_stores`, `ai_runtime`, `ai_routing`, `integration_stores`, `settings_vault` и `database`; `backend/src/domains/api_support/stores.rs` сокращен с 340 до 10 строк и оставлен как crate-local facade/re-export boundary, все новые stores modules меньше 120 строк. ADR-0073/0056 semantics сохранены: route handlers continue to import through the shared API support facade, router-level local API auth remains outside store helpers, AI runtime routing continues to use readiness-aware model selection, and database-not-configured errors keep the same `ApiError::DatabaseNotConfigured` boundary.
* [x] Mail rules boundary разделен на focused modules `models`, `mode`, `store`, `evaluation`, `rows`, `validation`, `errors` и `tests`; `backend/src/domains/mail/rules.rs` сокращен с 334 до 15 строк и оставлен как public facade/re-export boundary, все новые rule modules меньше 120 строк. ADR-0041/0046/0052/0055/0092 и Communications semantics сохранены: rules still operate over projected message metadata/content, no provider write or auto-execute capability is introduced, `RuleMode` string contract is unchanged, and existing condition/action parsing behavior is preserved.
* [x] Ollama runtime client boundary разделен на focused modules `config`, `catalog`, `chat`, `embeddings`, `transport`, `models`, `responses`, `sanitization` и `error`; `backend/src/integrations/ollama/client.rs` сокращен с 333 до 67 строк и оставлен как public facade/constructor boundary, все новые Ollama client modules меньше 120 строк. ADR-0009/0049/0081/0082 semantics сохранены: Ollama remains the default local AI runtime, remote providers remain opt-in outside this boundary, `/api/version`, `/api/tags`, `/api/chat` и `/api/embed` behavior stays unchanged, Qwen thinking-tag stripping remains local to chat response sanitization, and embedding shape validation remains covered by existing fake-server tests.
* [x] WhatsApp Web store boundary разделен на focused modules `accounts`, `sessions`, `ingestion`, `queries` и `intelligence`; `backend/src/integrations/whatsapp/client/store.rs` сокращен с 333 до 18 строк и оставлен как public store facade/constructor boundary, все новые store modules меньше 120 строк. ADR-0051 и Communications semantics сохранены: fixture/manual companion state remains the only implemented path, live WhatsApp runtime/sends remain outside this slice, raw WhatsApp records stay append-only with provider provenance, canonical projections still use `channel_kind = 'whatsapp_web'`, and Decision/Task candidate refresh remains source-backed from projected communication messages.
* [x] Telegram account store boundary разделен на focused modules `fixture_setup`, `live_setup`, `live_credentials`, `credential_bindings` и `lifecycle`; `backend/src/integrations/telegram/client/accounts.rs` сокращен с 330 до 5 строк и оставлен как client-account facade, все новые account modules меньше 120 строк. ADR-0050/0083/0091 и `docs/telegram/` semantics сохранены: fixture account setup, live/live-blocked setup, QR-authorized runtime metadata, account-scoped credential binding, host-vault secret payload boundary, logout/remove lifecycle states and evidence-preserving account removal behavior remain unchanged.
* [x] Decision model boundary разделен на focused modules `decision`, `entity_kind`, `source_kind`, `states`, `evidence` и `impacted_entity`; `backend/src/domains/decisions/models.rs` сокращен с 329 до 13 строк и оставлен как public facade/re-export boundary, все новые model modules меньше 120 строк. ADR-0089 и `docs/domains/decisions.md` semantics сохранены: durable Decisions remain source-backed, evidence and impacted-entity DTOs keep validation, status/review-state string contracts stay unchanged, candidate refresh remains reviewable, and Decision persistence still does not auto-create Tasks, Projects or Obligations.
* [x] Obligation model boundary разделен на focused modules `obligation`, `read_model`, `entity_kind`, `source_kind`, `states` и `evidence`; `backend/src/domains/obligations/models.rs` сокращен с 327 до 13 строк и оставлен как public facade/re-export boundary, все новые model modules меньше 120 строк. ADR-0088 и `docs/domains/obligations.md` semantics сохранены: durable Obligations remain source-backed with evidence, status/review/risk string contracts stay unchanged, beneficiary validation is preserved, obligation-derived task candidates remain reviewable, and confirmed Obligations still do not auto-create Tasks outside explicit task-candidate review.
* [x] Document attachment intelligence boundary разделен на focused modules `classification`, `file_kinds`, `models` и `tests`; `backend/src/domains/documents/attachment_intelligence.rs` сокращен с 325 до 50 строк и оставлен как public facade/classification service, все новые attachment intelligence modules меньше 120 строк. ADR-0046 и `docs/domains/documents.md` semantics сохранены: attachment intelligence остается metadata-only классификацией, blob bytes не попадают в PostgreSQL, attachment safety scanner boundary не меняется, no-op scanner не помечает вложения как `clean`, а executable/archive/document/source/image/category classification behavior сохранен существующими unit tests.
* [x] Organization workflows boundary разделен на focused modules `timeline`, `templates`, `portals`, `procedures`, `playbooks` и `errors`; `backend/src/domains/organizations/workflows.rs` сокращен с 313 до 13 строк и оставлен как public facade/re-export boundary, все новые workflow modules меньше 120 строк. ADR-0061/0064/0065/0066 и `docs/domains/organizations.md` semantics сохранены: Organizations остаются first-class collective actor memory anchors, timeline records remain source-backed, portals store secret references only, procedures/playbooks remain stored workflow knowledge without automatic execution, and this slice does not expand CRM/health compatibility surfaces.
* [x] Mail remote image proxy boundary разделен на focused modules `handler`, `url_policy`, `fetcher`, `dns`, `reference` и `errors`; `backend/src/domains/mail/handlers/remote_images.rs` сокращен с 311 до 8 строк и оставлен как crate-local facade/export boundary, все новые remote image modules меньше 120 строк. ADR-0041/0046/0055/0056 semantics сохранены: endpoint остается plain router-protected handler, remote image URL must still be referenced by the message HTML, private/local hosts and non-public IPs remain blocked, DNS fallback keeps public-address filtering, and this slice does not add provider writes, credential access or blob/database storage changes.
* [x] Persons profile handler boundary разделен на focused modules `legacy`, `personas`, `owner`, `actions`, `search` и `models`; `backend/src/domains/persons/handlers/profile.rs` сокращен с 310 до 12 строк и оставлен как crate-local handler facade/export boundary, все новые profile modules меньше 120 строк. ADR-0084/0086 semantics сохранены: `/api/v1/personas` Persona read/update path, Owner Persona assignment, legacy `/api/v1/persons` compatibility reads, favorite/notes/fingerprint enrichment actions и search routes keep the same contracts, while this slice does not add new Contact/CRM surfaces and keeps `/api/v1/persons` marked as compatibility in the Persona read model.
* [x] Documents core import boundary разделен на focused modules `models`, `store`, `validation`, `markdown`, `fingerprint`, `rows` и `errors`; `backend/src/domains/documents/core.rs` сокращен с 307 до 11 строк и оставлен как public facade/re-export boundary, все новые core modules меньше 120 строк. ADR-0017/0046 и `docs/domains/documents.md` semantics сохранены: markdown/pdf import contracts, deterministic local markdown fingerprint, PostgreSQL metadata/extracted-text storage, processing enqueue boundary, document-kind change rejection and no-heavy-blob/no-attachment-scanner behavior remain unchanged.
* [x] Persons trust boundary разделен на focused modules `promises`, `risks`, `obligation_projection`, `health_projection`, `rows`, `models` и `errors`; `backend/src/domains/persons/trust.rs` сокращен с 303 до 12 строк и оставлен как public facade/re-export boundary, все новые trust modules меньше 120 строк. ADR-0084/0086 и `docs/persons/architecture.md` semantics сохранены: person promises остаются compatibility source-backed commitment facts projected into Obligations, person risks remain Risk Engine observations with temporary `health_status` compatibility cache, and this slice does not add new `health`/`watchtower`, Contact/CRM, schema, route or Relationship persistence behavior.
* [x] Проведен дополнительный code review текущего Persons Trust SRP split diff и дополнен `frontend/docs/code-review-2026-06-14.md`: code-level regressions внутри split modules не подтверждены, но зафиксированы открытые review risks вокруг untracked split modules и live PostgreSQL coverage для promise/obligation и risk/health compatibility projection веток.
* [x] Dev email sync binary boundary разделен на focused modules `config`, `env`, `provider`, `account`, `checkpoint`, `fetch`, `runner`, `report` и `errors`; `backend/src/bin/hermes_email_sync_dev.rs` сокращен с 302 до 33 строк и оставлен как тонкий dev-only entrypoint, все новые modules меньше 120 строк. ADR-0032/0041/0042/0046/0055/0076/0080 semantics сохранены: command остается IMAP/iCloud-only dev cache sync, raw mail/blob bytes stay under local blob storage, PostgreSQL получает только provider metadata/checkpoints/projections, output report не содержит credential payloads, and env-provided mailbox password now lives in `ResolvedSecret` instead of plain `String`.
* [x] Проведен дополнительный code review текущего Dev Email Sync SRP split diff и дополнен `frontend/docs/code-review-2026-06-14.md`: code-level regressions внутри split modules не подтверждены, но зафиксированы открытые review risks вокруг untracked split modules и отсутствия live provider smoke validation в текущем окружении.
* [x] Mail communication query handler boundary разделен на focused modules `threads`, `search`, `personas` и `drafts`; `backend/src/domains/mail/handlers/communication_queries.rs` сокращен с 301 до 9 строк и оставлен как crate-local facade/re-export boundary, все новые communication query modules меньше 120 строк. ADR-0080/0085 и `docs/domains/communications.md` semantics сохранены: query handlers remain read/query/draft metadata surfaces, search still uses `HERMES_SEARCH_INDEX_PATH` when configured, draft CRUD remains local store behavior, local trash/provider write semantics are not changed, and no provider credential, raw MIME, attachment byte or provider mutation path is introduced.
* [x] Mail finance/analytics handler boundary дополнительно разделен на focused modules `invoices`, `analytics`, `explain` и `models`; `backend/src/domains/mail/handlers/finance_analytics.rs` сокращен с 144 до 9 строк и оставлен как crate-local facade/re-export boundary, все новые finance/analytics modules меньше 120 строк. Invoice DTOs moved from communication query boundary to finance-owned invoices module without route/API contract changes.
* [x] Проведен дополнительный code review текущего Mail Communication Queries / Finance Analytics SRP split diff и дополнен `frontend/docs/code-review-2026-06-14.md`: code-level regressions внутри split modules не подтверждены, но зафиксирован открытый review risk вокруг untracked split module directories до staging/commit.

## В работе

* [ ] Приведение реализации к документации без новых compatibility layers.
* [ ] Аудит существующих compatibility-модулей, legacy API и устаревших полей.
* [ ] Перевод интеграционных тестов на изолированный Testcontainers-подход.
* [ ] Устранение God Files и God Components перед добавлением новых mail/Telegram возможностей.

## Осталось реализовать

* [ ] Удалить compatibility storage вокруг `persons` и перейти на Persona-native schema.
* [ ] Удалить legacy Contact/Person CRM API и заменить их Persona-domain контрактами.
* [ ] Подключить Memory Engine к доменным источникам без compatibility cache.
* [ ] Реализовать durable read-model storage для Timeline Engine projections.
* [ ] Реализовать Enrichment Engine approved-source policy, conflict routing и cross-domain candidates.
* [ ] Реализовать Trust Engine contradiction inputs, review recommendations и cross-domain reconciliation.
* [ ] Реализовать Risk Engine cross-domain observations и review routing без терминов `health`/`watchtower`.
* [ ] Реализовать live-provider ingestion для Decisions, Obligations и Polygraph.
* [ ] Удалить или заменить функционал, который отсутствует в актуальной документации.
* [ ] Актуализировать все тесты под документацию как источник истины.
* [ ] Прогнать полный форматтер, линтеры, статический анализ и все тесты.

## Архитектурные проблемы

* В коде ещё есть compatibility layers вокруг `persons`, `health`, `watchtower`, legacy Person/Contact терминологии и старых API.
* По текущему scan в backend больше не осталось source files больше 700 строк; ближайшие production SRP-аудит кандидаты — `backend/src/domains/decisions/store.rs` на 301 строку, `backend/src/domains/graph/core/models.rs` на 298 строк, `backend/src/domains/mail/accounts/models.rs` на 295 строк, `backend/src/ai/core/runs.rs` на 290 строк, `backend/src/domains/organizations/api.rs` на 287 строк, `backend/src/domains/relationships/store.rs` на 285 строк, `backend/src/domains/obligations/store.rs` на 276 строк, `backend/src/domains/projects/link_reviews/store.rs` на 271 строку и `backend/src/domains/mail/threads.rs` на 270 строк. `backend/src/integrations/telegram/tdjson/tests.rs` на 561 строку является test-only спецификацией TDLib boundary и отслеживается отдельно от production facade.
* Полный `backend/tests/v1_communications_api.rs` на общем dev PostgreSQL сейчас не является надежным validation gate: `workflow_action_create_task_is_idempotent_and_records_safe_event` воспроизводимо возвращает 400 вместо 200 вне текущего `sending` boundary; требуется изолировать этот тест через Testcontainers/чистую БД или исправить workflow-action fixture.
* Во frontend больше не осталось Svelte-компонентов больше 500 строк по текущему scan.
* Во frontend больше не осталось source/service/store files больше 700 строк по текущему scan.
* Во frontend больше не осталось CSS files больше 700 строк по текущему scan; `app.css` сокращен до 640 строк, `pages.css` — до 457 строк, `sidebar.css` — до 590 строк, `panels.css` — до 697 строк.
* Часть интеграционных тестов зависит от общего dev-контейнера, а не от полного цикла Container → Migration → Fixture → Run → Destroy.
* Некоторые реализованные engine baseline ещё не подключены как полноценные доменные процессы.

## Следующий шаг

Продолжить приведение реализации к документации: выполнить SRP-аудит `backend/src/domains/decisions/store.rs` относительно ADR-0089 и `docs/domains/decisions.md`, сохранив source-backed Decision persistence, evidence/impact semantics, reviewability and avoiding automatic Task/Project/Obligation creation.
