[package]
name = "hermes-communication-cross-channel-forward-testkit"
version = "0.1.0"
edition = "2024"
license = "MIT"

[package.metadata.hermes]
role = "test"
owner = "test"
surface = "test_support"

[dev-dependencies]
hermes-communication-cross-channel-forward-core = { path = "../../../src/communication-cross-channel-forward-core" }
hermes-communication-cross-channel-forward-persistence = { path = "../../../src/communication-cross-channel-forward-persistence", features = ["conformance-test-support"] }
hermes-communication-delivery-intent-ingress-api = { path = "../../../src/communication-delivery-intent-ingress-api" }
hermes-communications-cross-channel-forward-source-api = { path = "../../../src/communications-cross-channel-forward-source-api" }
tokio = { version = "=1.52.4", default-features = false, features = ["macros", "rt-multi-thread"] }
