[package]
name = "hermes-mail-contacts-sync-testkit"
version = "0.1.0"
edition = "2024"
license = "MIT"

[package.metadata.hermes]
role = "test"
owner = "test"
surface = "test_support"

[dev-dependencies]
hermes-mail-contacts-sync-core = { path = "../../../src/mail-contacts-sync-core" }
hermes-mail-contacts-sync-persistence = { path = "../../../src/mail-contacts-sync-persistence", features = ["conformance-test-support"] }
sha2 = { version = "=0.11.0", default-features = false }
tokio = { version = "=1.52.4", default-features = false, features = ["macros", "rt-multi-thread"] }
