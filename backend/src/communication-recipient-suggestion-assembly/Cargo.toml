[package]
name = "hermes-communication-recipient-suggestion-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "hermes-communication-recipient-suggestion-assembly"
path = "src/main.rs"

[package.metadata.hermes]
role = "workflow"
owner = "communication_recipient_suggestion"
surface = "assembly"

[dependencies]
hermes-communication-recipient-suggestion-persistence = { path = "../communication-recipient-suggestion-persistence" }
hermes-communication-recipient-suggestion-runtime = { path = "../communication-recipient-suggestion-runtime" }
hermes-runtime-protocol = { path = "../platform/runtime_protocol" }
hermes-storage-protocol = { path = "../platform/storage/protocol" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"

