[package]
name = "hermes-communication-summary-core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.hermes]
role = "workflow"
owner = "communication_summary"
surface = "implementation"

[dependencies]
