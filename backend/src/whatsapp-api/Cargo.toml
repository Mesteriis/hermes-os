[package]
name = "hermes-whatsapp-api"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.hermes]
role = "integration"
owner = "whatsapp"
surface = "contract"

[dependencies]
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive"] }

[build-dependencies]
prost-build = "=0.14.4"
protoc-bin-vendored = "=3.2.0"
