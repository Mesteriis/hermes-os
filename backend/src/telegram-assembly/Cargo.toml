[package]
name = "hermes-telegram-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "hermes-telegram-assembly"
path = "src/main.rs"

[package.metadata.hermes]
role = "integration"
owner = "telegram"
surface = "assembly"

[dependencies]
hermes-runtime-protocol = { path = "../platform/runtime_protocol" }
hermes-storage-protocol = { path = "../platform/storage/protocol" }
hermes-telegram-automation-persistence = { path = "../telegram-automation-persistence" }
hermes-telegram-calls-persistence = { path = "../telegram-calls-persistence" }
hermes-telegram-persistence = { path = "../telegram-persistence" }
hermes-telegram-runtime = { path = "../telegram-runtime" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
