[package]
name = "hermes-whisper-stt-core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.hermes]
role = "integration"
owner = "whisper_stt"
surface = "implementation"

[dependencies]
hermes-speech-to-text-api = { path = "../speech-to-text-api" }
hermes-speech-transcript-artifact = { path = "../speech-transcript-artifact" }
