[package]
name = "hermes-scheduler"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[package.metadata.hermes]
role = "platform"
owner = "scheduler"
surface = "implementation"

[dependencies]
hermes-clock-protocol = { path = "../../clock/protocol" }
hermes-events-protocol = { path = "../../events" }
hermes-scheduler-protocol = { path = "../protocol" }
prost = "=0.14.4"
prost-types = "=0.14.4"
