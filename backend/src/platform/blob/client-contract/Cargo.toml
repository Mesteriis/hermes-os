[package]
name = "hermes-blob-client-contract"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.hermes]
role = "platform"
owner = "blob"
surface = "contract"

[dependencies]
hermes-runtime-protocol = { path = "../../runtime_protocol" }
