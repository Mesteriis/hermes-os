[package]
name = "hermes-runtime-protocol"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[package.metadata.hermes]
role = "platform"
owner = "runtime_protocol"
surface = "contract"

[dependencies]
prost = "=0.14.4"
prost-types = "=0.14.4"

[build-dependencies]
prost-build = "=0.14.4"
protoc-bin-vendored = "=3.2.0"
