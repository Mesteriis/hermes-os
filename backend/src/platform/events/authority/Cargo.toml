[package]
name = "hermes-events-authority"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[package.metadata.hermes]
role = "platform"
owner = "events"
surface = "implementation"

[dependencies]
hermes-events-jetstream = { path = "../jetstream" }
zeroize = "=1.9.0"
