[package]
name = "hermes-storage-protocol"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[package.metadata.hermes]
role = "platform"
owner = "storage"
surface = "contract"

[dependencies]
prost = "=0.14.4"
sha2 = { version = "=0.11.0", default-features = false }

[build-dependencies]
prost-build = "=0.14.4"
protoc-bin-vendored = "=3.2.0"
