[package]
name = "hermes-storage-control"
version = "0.1.0"
edition = "2024"
license = "MIT"
[lib]
path = "src/lib.rs"
[package.metadata.hermes]
role = "platform"
owner = "storage"
surface = "implementation"
[dependencies]
hermes-storage-protocol = { path = "../protocol" }
