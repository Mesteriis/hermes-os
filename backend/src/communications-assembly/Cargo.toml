[package]
name = "hermes-communications-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "hermes-communications-assembly"
path = "src/main.rs"

[package.metadata.hermes]
role = "domain"
owner = "communications"
surface = "assembly"

[dependencies]
hermes-communications-persistence = { path = "../communications-persistence" }
hermes-communications-runtime = { path = "../communications-runtime" }
hermes-runtime-protocol = { path = "../platform/runtime_protocol" }
hermes-storage-protocol = { path = "../platform/storage/protocol" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
