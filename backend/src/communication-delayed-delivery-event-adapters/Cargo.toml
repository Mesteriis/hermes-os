[package]
name = "hermes-communication-delayed-delivery-event-adapters"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.hermes]
role = "workflow"
owner = "communication_delayed_delivery"
surface = "implementation"

[dependencies]
hermes-communication-delayed-delivery-api = { path = "../communication-delayed-delivery-api" }
hermes-events-protocol = { path = "../platform/events" }
hermes-scheduler-protocol = { path = "../platform/scheduler/protocol" }
prost = "=0.14.4"
prost-types = "=0.14.4"
sha2 = { version = "=0.11.0", default-features = false }
