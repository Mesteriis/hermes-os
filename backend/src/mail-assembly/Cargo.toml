[package]
name = "hermes-mail-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "hermes-mail-assembly"
path = "src/main.rs"

[package.metadata.hermes]
role = "integration"
owner = "mail"
surface = "assembly"

[dependencies]
hermes-mail-persistence = { path = "../mail-persistence" }
hermes-mail-runtime = { path = "../mail-runtime" }
hermes-runtime-protocol = { path = "../platform/runtime_protocol" }
hermes-storage-protocol = { path = "../platform/storage/protocol" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
