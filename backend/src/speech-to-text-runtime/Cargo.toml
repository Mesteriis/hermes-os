[package]
name = "hermes-speech-to-text-runtime"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "hermes-speech-to-text-runtime"
path = "src/main.rs"

[package.metadata.hermes]
role = "engine"
owner = "speech_to_text"
surface = "runtime"

[dependencies]
hermes-blob-client = { path = "../platform/blob/client" }
hermes-runtime-protocol = { path = "../platform/runtime_protocol" }
hermes-speech-to-text-api = { path = "../speech-to-text-api" }
hermes-speech-to-text-core = { path = "../speech-to-text-core" }
hermes-speech-to-text-persistence = { path = "../speech-to-text-persistence" }
hermes-storage-protocol = { path = "../platform/storage/protocol" }
hermes-storage-vault = { path = "../platform/storage/vault" }
libc = "=0.2.186"
prost = "=0.14.4"
sha2 = { version = "=0.11.0", default-features = false }
tokio = { version = "=1.52.4", default-features = false, features = ["rt-multi-thread", "time"] }
zeroize = "=1.9.0"
