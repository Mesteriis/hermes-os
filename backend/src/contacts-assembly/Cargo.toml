[package]
name = "hermes-contacts-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "hermes-contacts-assembly"
path = "src/main.rs"

[package.metadata.hermes]
role = "domain"
owner = "contacts"
surface = "assembly"

[dependencies]
hermes-contacts-persistence = { path = "../contacts-persistence" }
hermes-contacts-runtime = { path = "../contacts-runtime" }
hermes-runtime-protocol = { path = "../platform/runtime_protocol" }
hermes-storage-protocol = { path = "../platform/storage/protocol" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
