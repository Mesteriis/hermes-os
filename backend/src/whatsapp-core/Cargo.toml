[package]
name = "hermes-whatsapp-core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.hermes]
role = "integration"
owner = "whatsapp"
surface = "implementation"

[dependencies]
hermes-communications-ingress = { path = "../communications-ingress" }
hermes-whatsapp-api = { path = "../whatsapp-api" }
