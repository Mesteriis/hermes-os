[package]
name = "hermes-mail-contacts-sync-core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.hermes]
role = "workflow"
owner = "mail_contacts_sync"
surface = "implementation"
