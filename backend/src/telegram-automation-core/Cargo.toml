[package]
name = "hermes-telegram-automation-core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.hermes]
role = "integration"
owner = "telegram"
surface = "automation-core"

[dependencies]
sha2 = { version = "=0.10.9", default-features = false }
