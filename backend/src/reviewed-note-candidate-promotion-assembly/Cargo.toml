[package]
name = "hermes-reviewed-note-candidate-promotion-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "hermes-reviewed-note-candidate-promotion-assembly"
path = "src/main.rs"

[package.metadata.hermes]
role = "workflow"
owner = "reviewed_note_candidate_promotion"
surface = "assembly"

[dependencies]
hermes-reviewed-note-candidate-promotion-persistence = { path = "../reviewed-note-candidate-promotion-persistence" }
hermes-reviewed-note-candidate-promotion-runtime = { path = "../reviewed-note-candidate-promotion-runtime" }
hermes-runtime-protocol = { path = "../platform/runtime_protocol" }
hermes-storage-protocol = { path = "../platform/storage/protocol" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
