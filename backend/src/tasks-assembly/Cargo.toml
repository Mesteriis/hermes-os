[package]
name = "hermes-tasks-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "hermes-tasks-assembly"
path = "src/main.rs"

[package.metadata.hermes]
role = "domain"
owner = "tasks"
surface = "assembly"

[dependencies]
hermes-tasks-persistence = { path = "../tasks-persistence" }
hermes-tasks-runtime = { path = "../tasks-runtime" }
hermes-runtime-protocol = { path = "../platform/runtime_protocol" }
hermes-storage-protocol = { path = "../platform/storage/protocol" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
