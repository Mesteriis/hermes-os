[package]
name = "hermes-kernel-control-store"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.hermes]
role = "core"
owner = "kernel"
surface = "contract"
[dependencies]
sha2 = { version = "=0.11.0", default-features = false }
