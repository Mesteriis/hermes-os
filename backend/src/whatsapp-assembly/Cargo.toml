[package]
name = "hermes-whatsapp-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "hermes-whatsapp-assembly"
path = "src/main.rs"

[package.metadata.hermes]
role = "integration"
owner = "whatsapp"
surface = "assembly"

[dependencies]
hermes-runtime-protocol = { path = "../platform/runtime_protocol" }
hermes-storage-protocol = { path = "../platform/storage/protocol" }
hermes-whatsapp-persistence = { path = "../whatsapp-persistence" }
hermes-whatsapp-runtime = { path = "../whatsapp-runtime" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
