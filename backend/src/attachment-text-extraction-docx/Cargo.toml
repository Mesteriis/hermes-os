[package]
name = "hermes-attachment-text-extraction-docx"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.hermes]
role = "workflow"
owner = "attachment_text_extraction"
surface = "implementation"

[dependencies]
hermes-attachment-text-extraction-parser-contract = { path = "../attachment-text-extraction-parser-contract" }
quick-xml = { version = "=0.41.0", default-features = false }
zip = { version = "=6.0.0", default-features = false, features = ["deflate-flate2-zlib-rs"] }
