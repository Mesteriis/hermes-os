[package]
name = "hermes-communication-delayed-delivery-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "hermes-communication-delayed-delivery-assembly"
path = "src/main.rs"

[package.metadata.hermes]
role = "workflow"
owner = "communication_delayed_delivery"
surface = "assembly"

[dependencies]
hermes-communication-delayed-delivery-persistence = { path = "../communication-delayed-delivery-persistence" }
hermes-communication-delayed-delivery-runtime = { path = "../communication-delayed-delivery-runtime" }
hermes-runtime-protocol = { path = "../platform/runtime_protocol" }
hermes-storage-protocol = { path = "../platform/storage/protocol" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
