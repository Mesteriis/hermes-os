[package]
name = "hermes-attachment-preview-evidence-replay-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "hermes-attachment-preview-evidence-replay-assembly"
path = "src/main.rs"

[package.metadata.hermes]
role = "workflow"
owner = "attachment_preview_evidence_replay"
surface = "assembly"

[dependencies]
hermes-attachment-preview-evidence-replay-persistence = { path = "../attachment-preview-evidence-replay-persistence" }
hermes-attachment-preview-evidence-replay-runtime = { path = "../attachment-preview-evidence-replay-runtime" }
hermes-runtime-protocol = { path = "../platform/runtime_protocol" }
hermes-storage-protocol = { path = "../platform/storage/protocol" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
