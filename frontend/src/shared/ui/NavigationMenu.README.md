# NavigationMenu

Horizontal product navigation primitive.

Use for top-level local navigation groups.

Do not encode route names, domain stores, or provider-specific destinations here.

Accessibility: renders a labelled `nav` and marks the current item.
