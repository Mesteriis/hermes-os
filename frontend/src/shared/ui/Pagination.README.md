# Pagination

Tokenized page navigation control.

Use for local page sets with a known page count.

Do not put data fetching or cursor handling inside this component.

Accessibility: renders labelled navigation and marks the active page with `aria-current="page"`.
