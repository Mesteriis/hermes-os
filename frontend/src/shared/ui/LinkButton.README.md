# LinkButton

Anchor rendered with button-like low-emphasis styling.

Use for navigation or external references where the action changes location.

Do not use for state mutation; use `Button` or `TextButton` for commands.

Accessibility: disabled links remove `href` and expose `aria-disabled`. External links set `target="_blank"` and `rel="noreferrer"`.
