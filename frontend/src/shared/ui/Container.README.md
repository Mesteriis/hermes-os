# Container

Width-constrained layout primitive.

Use to center and constrain Storybook examples or product surfaces without domain logic.

Do not use for grids, stacks, or business-specific page composition.

Accessibility: it is layout-only and should not replace semantic landmarks when those are required.
