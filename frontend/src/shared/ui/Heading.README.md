# Heading

Semantic heading primitive for UI surfaces.

Use for section titles and hierarchy inside cards, panels, dialogs, sheets, and Storybook examples.

Do not use for visual-only bold text; use `Text` with an appropriate weight instead.

Accessibility: keep heading levels ordered within a surface. Use `as` only when visual size and semantic level need to differ.
