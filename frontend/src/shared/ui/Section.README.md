# Section

Generic page or panel section.

Use for semantic content regions with optional header, actions and footer slots.

Do not use as a card; use `Card`, `Panel` or `Paper` when the content needs a framed surface.

Accessibility: choose the `as` prop for the correct document outline and provide meaningful heading content in the `header` slot.
