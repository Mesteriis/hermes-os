# ToolbarSection

Labelled section inside toolbars and dense control rows.

Use to separate related controls within a local toolbar without creating a full panel.

Do not use as page layout or global navigation.

Accessibility: keep labels short and pair with an enclosing toolbar label when the controls need broader context.
