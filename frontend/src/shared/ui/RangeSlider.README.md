# RangeSlider

Two-thumb range control built from native range inputs.

Use for selecting a minimum and maximum value in a bounded UI-only range.

Do not use for domain-specific time or money ranges without a higher-level wrapper.

Accessibility: renders two native range inputs and preserves min <= max.
Use `minLabel` and `maxLabel` when the defaults are not specific enough.
