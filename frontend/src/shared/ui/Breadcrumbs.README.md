# Breadcrumbs

Hierarchical navigation trail.

Use for showing where a surface sits inside an application hierarchy.

Do not use for domain workflow state or provider-specific navigation.

Accessibility: renders a labelled `nav` with `aria-current="page"` on the current item.
