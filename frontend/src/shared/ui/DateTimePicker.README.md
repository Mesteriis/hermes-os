# DateTimePicker

Native datetime-local input.

Use for local UI capture of a date and time.

Do not use as a canonical timestamp boundary; owners must normalize timezones.

Accessibility: preserves browser datetime-local semantics and supports `min` and `max`.
