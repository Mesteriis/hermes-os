# Spacer

Presentation-only flex spacer.

`orientation="horizontal"` consumes available inline space in a row layout.
`orientation="vertical"` consumes available block space in a column layout. When
multiple spacers share the same flex container, they divide free space evenly.
