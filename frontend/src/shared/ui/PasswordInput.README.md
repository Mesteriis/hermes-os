# PasswordInput

Password input with local show/hide visibility toggle.

Use for credential setup forms and local secret-entry surfaces.

Do not store or log password values in this component.

Accessibility: the visibility toggle is a native button with a changing accessible label.
