# Slider

Single-value range input.

Use for UI-only numeric adjustment where approximate interaction is acceptable.

Do not use when precise typed input is required without pairing with `NumberInput`.

Accessibility: renders a native range input and can expose a visible value.
