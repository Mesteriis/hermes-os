# MultiSelect

Native multiple selection control with selected-value chips.

Use when several independent options can be selected from a compact list.

Do not use for large remote datasets or provider-backed search.

Accessibility: uses the native `select multiple` interaction and exposes selected labels in a polite live region.
