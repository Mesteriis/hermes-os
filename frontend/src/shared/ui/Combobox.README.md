# Combobox

Text input with native datalist suggestions.

Use when users may type a custom value or choose a known option.

Do not use for backend autocomplete or command-palette navigation.

Accessibility: keeps native input semantics and links suggestions through `list`.
