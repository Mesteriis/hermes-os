# SplitButton

UI-only compound action control with a primary action and an adjacent menu. Menu
items are local `NavigationItem` data; the component emits selection events and
does not execute provider or domain commands by itself.
