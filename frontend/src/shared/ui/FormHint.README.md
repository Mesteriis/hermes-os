# FormHint

Supporting form helper text.

Use for short guidance that improves completion speed.

Do not use for validation failure; use `FormError`.

Accessibility: provide an `id` and connect it with `aria-describedby` when needed.
