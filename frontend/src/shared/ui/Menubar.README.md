# Menubar

Desktop-style menubar primitive.

Use for dense app-level command groups.

Do not use for hidden provider or remote actions without an owning workflow.

Accessibility: uses menubar/menu/menuitem roles and exposes submenus on hover or focus.
