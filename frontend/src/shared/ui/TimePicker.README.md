# TimePicker

Native time input.

Use for local time selection inside a form.

Do not embed timezone or calendar-domain behavior here.

Accessibility: preserves browser time input semantics and supports `min`, `max` and `step`.
