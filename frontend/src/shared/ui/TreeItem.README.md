# TreeItem

Recursive tree item primitive used by `Tree`.

Use when a tree needs a custom item surface.

Do not import it from domains directly unless a domain wrapper owns the data mapping.

Accessibility: supports Enter, Space, ArrowRight and ArrowLeft for local expand/collapse behavior.
