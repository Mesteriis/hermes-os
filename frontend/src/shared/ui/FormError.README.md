# FormError

Form validation error text.

Use for user-correctable validation messages.

Do not use for system failures, provider errors, or toast notifications.

Accessibility: renders with `role="alert"`.
