# RadioGroup

Grouped radio control context.

Use for mutually exclusive UI-only choices.

Do not use for multi-select, menus, or navigation tabs.

Accessibility: renders a `fieldset` with optional legend text.
