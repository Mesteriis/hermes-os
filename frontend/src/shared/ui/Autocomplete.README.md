# Autocomplete

Tokenized local autocomplete with keyboard navigation.

Use for small local option sets where users need filtered suggestions.

Do not use for remote search, provider data, or durable business selection without an owning wrapper.

Accessibility: exposes combobox/listbox roles and supports Arrow keys, Home, End, Enter and Escape.
