# OTPInput

Segmented one-time-code input.

Use for short local verification codes and compact token entry.

Do not use for authentication workflows directly; owning forms handle validation and submission.

Accessibility: renders a grouped set of native text inputs with per-cell labels and arrow-key movement.
