# ContextMenu

Context action surface for UI-only local actions.

Use for nearby actions that can also be exposed elsewhere.

Do not hide the only path to destructive or provider-side behavior inside this component.

Accessibility: opens from click or contextmenu, uses `role="menu"` and closes with Escape.
