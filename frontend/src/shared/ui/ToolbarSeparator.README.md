# ToolbarSeparator

Presentation-only separator for toolbar command clusters.

Use it between `ToolbarGroup` instances to keep dense command surfaces visually
consistent without hand-rolled divider markup in every domain component.
