# FilePicker

Native file input.

Use for local UI file selection.

Do not upload files, parse provider exports, or store bytes from this component.

Accessibility: preserves browser file input semantics and emits selected files to the owning form.
