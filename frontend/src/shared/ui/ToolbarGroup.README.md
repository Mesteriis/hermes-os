# ToolbarGroup

Presentation-only wrapper for related toolbar controls.

Use it inside `Toolbar` or a domain toolbar when several actions belong to the
same command cluster. The component does not own behavior; children still emit
their own events.
