# Radio

Native radio option for `RadioGroup` or standalone controlled use.

Use for one option in a mutually exclusive set.

Do not use for checkbox-like independent selections.

Accessibility: renders a native radio input inside a label.
