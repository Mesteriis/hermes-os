# ColorPicker

Native color input paired with an editable hex value field.

Use for local theme and visualization controls.

Do not use for semantic status colors; those should come from design tokens.

Accessibility: each input has a label and the text field keeps the value inspectable.
