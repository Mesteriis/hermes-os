# Tree

Hierarchical tree container for local navigation items.

Use for compact nested navigation and picker structures.

Do not use for domain graph traversal or provider folder synchronization.

Accessibility: renders `tree`, `treeitem`, `group`, `aria-expanded`, and selection state.
