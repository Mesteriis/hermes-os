# SearchPalette

Search-focused command palette wrapper.

Use for local searchable navigation and action discovery.

Do not wire it directly to provider search or backend APIs.

Accessibility: delegates dialog, input and keyboard behavior to `CommandPalette`.
