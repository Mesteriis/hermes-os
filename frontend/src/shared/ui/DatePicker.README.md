# DatePicker

Native date input.

Use for UI-only date selection in forms.

Do not encode domain scheduling rules inside this component.

Accessibility: preserves browser date input semantics and supports `min` and `max`.
