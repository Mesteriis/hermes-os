# Panel

Tokenized lightweight surface for grouped UI.

Use for grouped controls, inspectors, and local context areas.

Do not use as a business domain component or nested decorative card.

Clipping is off by default so overlays can escape the panel surface. Use `clip`
only for media, masks, or intentional visual clipping.

Accessibility: choose the `as` prop to match the section's semantic role.
