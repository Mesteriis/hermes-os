# SearchInput

Search input with leading search icon and optional clear action.

Use for local filtering, command surfaces, and UI-only search fields.

Do not use for provider search behavior or backend query orchestration.

Accessibility: provide `ariaLabel` when no visible label is present. Clear action has a separate accessible label.
