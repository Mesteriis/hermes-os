# CharacterCounter

Tokenized character count display.

Use beside text inputs and textareas where length constraints matter.

Do not enforce limits here; enforcement belongs in the owning form logic.

Accessibility: announces changes politely with `aria-live`.
