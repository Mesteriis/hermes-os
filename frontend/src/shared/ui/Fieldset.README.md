# Fieldset

Semantic grouping for form and control clusters.

Use when controls share one label, description or disabled state.

Do not use as a decorative surface around unrelated controls.

Accessibility: provide a `legend` slot whenever the grouped controls need a shared accessible label.
