{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Recovery shell permissions only; provider host bridges are isolated to their companion windows.",
  "windows": [
    "main"
  ],
  "permissions": ["core:default"]
}
